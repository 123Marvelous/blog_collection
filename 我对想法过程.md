```我的想法过程
在当前文件夹做一个可玩的最小 Web 沙盒：

- 只要一个 index.html（Three.js 用 CDN）

- 2d横向，WASD 移动，鼠标锁定视角，打怪（游玩角色与怪与npc（这里怎么跟cursor说（写的代码考虑后面更换皮肤素材，或者跑通之后可以替换方块））），远近武器就需要个方块形状即可

- 左键挖轻击，右键放蓄力（一点时间，伤害与时长成正比（模块化（这里要适配不同武器）便于使用不同武器（同样触发这样部分（的不同表现形式（例如近战武器蓄力缓慢伤害（在一定范围内，范围作为蓄力加成封顶）随时间呈现正相关）））））

- 用彩色方块即可，不要外部图片/模型，不要打包工具，只考虑最小执行，目的是跑通一次看看效果。

做完告诉我怎么在浏览器打开。
```

实际过程
```实际过程
在当前文件夹做一个最小可玩的 Web 打怪 demo：只要一个 index.html，Three.js 用 CDN，不要打包、不要外部图片/模型。

【视角与操作】
- 2D 横版（侧视），相机跟着玩家
- A/D 左右移动，W 或空格跳跃
- 左键：轻攻击；按住右键蓄力松开：重攻击（伤害随蓄力时间增加，设上限）（远程武器左右都是攻击右键增加后摇时间（右键伤害会增加一些（随机增加，有上限范围）））
- 视角（武器角度）跟随鼠标方向

【内容（先最小）】
- 1 个玩家、若干同种怪物、1 个 NPC
- 怪物会靠近玩家并攻击；打死会消失或掉简单分数
- NPC 靠近按 E 显示一句对话即可（可先不做任务）

【结构（为以后换皮肤/素材，避免大改）】
- 用实体数据：{ id, type, x, y, hp, atk, faction }若干只同种怪物（先实现一种id即可后续按需求增加；种类差异只放在数据里，如 hp/atk/颜色）
- 显示与逻辑分离：createView(entity) 现在只用彩色方块/长方体占位；
  以后替换皮肤只改 createView（或资源表），不要改移动/战斗/AI
- 攻击统一走 applyDamage(attacker, target, amount)
- 武器先做一个近战 hitbox（矩形即可）；不要做多种武器，但接口预留 weaponId
- 代码写在同一 index.html 里，用清晰注释分区：数据 / 渲染 / 输入 / 战斗 / AI

【验收】
浏览器打开就能玩：能移动跳跃、能打怪掉血、能蓄力重击、能和 NPC 简单交互。
完成后告诉我怎么打开。
```